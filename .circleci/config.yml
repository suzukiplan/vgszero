version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "install build-essential"
          command: "sudo apt-get update && sudo apt-get install -y build-essential"
      - run:
          name: "install sdcc"
          command: "sudo apt update && sudo apt install -y sdcc"
      - run:
          name: "Build and Test VGS-Zero"
          command: "make tests"

workflows:
  test-workflow:
    jobs:
      - test
